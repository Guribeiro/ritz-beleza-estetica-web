import { Container } from './styles';

const Footer = (): JSX.Element => {
  return (
    <Container>
      <section>
        <h1>footer</h1>
      </section>
    </Container>
  );
};

export default Footer;
